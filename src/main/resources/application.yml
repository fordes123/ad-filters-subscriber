spring:
  application:
    name: "@project.name@"
    version: "@project.version@"
  profiles:
    active: dev

logging:
  file:
    path: ./logs

application:
  input:
  config:
    tracking:
      enable: true
      path: logs/lost.txt