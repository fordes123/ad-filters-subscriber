spring:
  application:
    name: ad-filters-subscriber
  profiles:
    active: dev

logging:
  file:
    path: ./logs

application:
  input:
  config:
    tracking:
      enable: true
      path: logs/lost.txt